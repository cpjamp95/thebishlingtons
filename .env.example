# Public client credentials only. NEVER put a service-role / secret key here.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Optional local-only fictional preview. Ignored by production builds.
PUBLIC_ONBOARDING_PREVIEW=false
